import { Input } from 'components';

export const Primary = () => <Input label='Email Address' type='warning' />;

const InputStory = {
	title: 'Design System/Input',
	component: Input,
};

export default InputStory;
